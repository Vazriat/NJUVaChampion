# ============================================================================
# 换行符策略
#
# Windows 默认 core.autocrlf=true，会在检出时把文本文件转成 CRLF。对 Java/TS
# 无害，但对要在 Linux 上执行的东西是致命的：
#   - shell 脚本：shebang 变成 `#!/usr/bin/env bash\r`，报
#     "bad interpreter: No such file or directory"
#   - nginx 配置 / Dockerfile：会出现难以定位的语法错
#
# 这里显式锁定这些文件为 LF，不依赖每个开发者的 autocrlf 设置。
# ============================================================================

*.sh           text eol=lf
*.conf         text eol=lf
*.example      text eol=lf
*.yml          text eol=lf
*.yaml         text eol=lf
Dockerfile     text eol=lf
Dockerfile.*   text eol=lf
*.dockerfile   text eol=lf
